const images = document.querySelector('.galleryPic');
const shadow = document.querySelector('.shadow');

let displayBigImage = function(image) {
    shadow.style.opacity = '0.75';
    
}

images.forEach(element => {
    element.addEventListener('click', )
});